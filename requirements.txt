flask>=3.0
pdfplumber>=0.10
deep-translator>=1.11
reportlab>=4.0
requests>=2.31
python-docx>=1.1
beautifulsoup4>=4.12
lxml>=5.0
sarvamai              # Sarvam AI official SDK
google-genai          # For Gemini engine (free API key at aistudio.google.com)

# Local AI engine (NLLB-200) — optional but recommended; ~2.3 GB model download on first use
# pip install torch transformers sentencepiece
# sentencepiece        # required tokenizer for NLLB-200
